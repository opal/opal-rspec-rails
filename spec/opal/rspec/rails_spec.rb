require 'spec_helper'

describe Opal::Rspec::Rails do
  it 'has a version number' do
    expect(Opal::Rspec::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
